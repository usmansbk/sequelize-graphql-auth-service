type Role {
  id: ID!
  name: AuthRole!
  description: String
  permissions: PermissionList!
  members: UserList!
  createdAt: DateTime!
  updatedAt: DateTime
}

# Pagination

type RoleList implements List {
  items: [Role]!
  nextCursor: ID
  totalCount: Int!
}
