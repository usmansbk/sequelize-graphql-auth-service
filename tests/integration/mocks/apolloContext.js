const context = (extraOptions) => ({
  // t,
  // jwt,
  // otp,
  // store,
  // locale,
  // clientId,
  // sessionId,
  // tokenInfo,
  // accessToken,
  // fileStorage,
});

export default context;
